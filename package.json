{
  "name": "line-density",
  "version": "0.0.1",
  "description": "Fast density heatmaps for massive time series.",
  "main": "build/index.js",
  "author": "Dominik Moritz",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/ndarray": "^1.0.6",
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.9.4",
    "rollup": "^0.62.0",
    "rollup-plugin-commonjs": "^9.1.3",
    "rollup-plugin-node-resolve": "^3.3.0",
    "typescript": "^2.9.2",
    "vega-embed": "^3.16.0"
  },
  "scripts": {
    "start": "parcel demo/index.html",
    "prebuild": "rm -rf build",
    "build:tsc": "tsc src/index.ts --outDir build --allowSyntheticDefaultImports --esModuleInterop -t es6",
    "build:rollup": "rollup -c",
    "build": "npm run build:tsc && npm run build:rollup",
    "postbuild": "uglifyjs build/line-density.js -c -m -o build/line-density.min.js",
    "predeploy": "rm -rf dist && parcel build --public-url '.' demo/index.html",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "ndarray": "^1.0.18",
    "regl": "^1.3.7"
  }
}
