export const NUM_SERIES = 1000;
export const NUM_POINTS = 64;

export const WIDTH = 16;
export const HEIGHT = 8;

export const CHART_WIDTH = 800;
export const CHART_HEIGHT = (CHART_WIDTH / WIDTH) * HEIGHT; // square rects

export const MAX_REPEATS_X = Infinity;
export const MAX_REPEATS_Y = Infinity;

export const USE_REAL_RANDOM = false;

export const DEBUG_CANVAS = false;
